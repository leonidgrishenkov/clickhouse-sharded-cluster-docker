# Clickhouse Sharded Cluster in Docker

Clickhouse sharded cluster running in docker on a single node.

I personally use it in order to test things locally as if it was a real multi-node cluster.

## Run the Cluster

To run the cluster:

```sh
docker compose up -d
```
